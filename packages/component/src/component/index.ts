export type ImageListData = {
  type: string;
  name: string;
}
import ReportEditor, { OnSaveHandler, OnChangeTitleHandler } from './reportEditor/ReportEditor';
import Report from './report/report/Report';
export { ReportEditor, Report }
export type { OnSaveHandler, OnChangeTitleHandler }
